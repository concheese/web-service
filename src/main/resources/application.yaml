spring:
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: true
    show-sql: true

management:
  endpoints:
    web:
      exposure:
        include: '*'

logging:
  level:
    org:
      springframework:
        context:
          annotation: TRACE
        web: DEBUG
        nodeValue: INFO